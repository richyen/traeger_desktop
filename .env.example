# Traeger Authentication
# Option 1: Use email/password (simplest)
TRAEGER_EMAIL=your.email@example.com
TRAEGER_PASSWORD=your_password

# Option 2: Use token directly (if you have one)
# TRAEGER_TOKEN=your_jwt_token_here

# Optional: Polling interval in seconds (default: 30)
# POLL_INTERVAL=30
